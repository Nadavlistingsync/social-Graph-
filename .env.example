# Server-only credentials (never commit real values)
# Used by /api/* — not shipped to the browser.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenRouter — free models work with a $0 key (default below)
OPENROUTER_API_KEY=
# OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free
# OPENROUTER_MODEL_FALLBACKS=openrouter/free,nvidia/nemotron-nano-9b-v2:free
