# Server-only Supabase credentials (never commit real values)
# Used by /api/auth and /api/graph — not shipped to the browser.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
