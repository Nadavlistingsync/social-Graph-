# Optional — contact import OAuth (these ARE client-side; set in Vercel if needed)
VITE_GOOGLE_CLIENT_ID=
VITE_MICROSOFT_CLIENT_ID=

# Supabase lives in the repo-root /.env (server only) — see /.env.example
# Do not put SUPABASE_SERVICE_ROLE_KEY or SUPABASE_ANON_KEY in VITE_ vars.
